import { users } from './users';

// Lets keep only the CRUD template features here for now, for other cases,
// we can import the feature directly in the page example.
export const features = {
  users: users,
};
